apiVersion: apps/v1
kind: Deployment
metadata:
  name: prod-geovany-welch-chouette-frontend-deployment
  namespace: prod-geovany-welch-chouette-frontend
  labels:
    app: prod-geovany-welch-chouette-frontend
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: prod-geovany-welch-chouette-frontend
  template:
    metadata:
      labels:
        app: prod-geovany-welch-chouette-frontend
    spec:
      containers:
        - name: prod-geovany-welch-chouette-frontend
          image: corentin35/geovany-welch-chouette-frontend:${TAG_VERSION}
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: '1'
              memory: '1Gi'
            limits:
              cpu: '2'
              memory: '2Gi'
      imagePullSecrets:
        - name: regcred
