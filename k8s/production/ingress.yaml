apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prod-geovany-welch-chouette-frontend-ingress
  namespace: prod-geovany-welch-chouette-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - chouette.flapi.org
      secretName: prod-geovany-welch-chouette-frontend-tls
  rules:
    - host: chouette.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: prod-geovany-welch-chouette-frontend-service
                port:
                  number: 3000
