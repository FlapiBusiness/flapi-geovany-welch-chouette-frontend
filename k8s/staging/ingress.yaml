apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: staging-geovany-welch-chouette-frontend-ingress
  namespace: staging-geovany-welch-chouette-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - staging.chouette.flapi.org
      secretName: staging-geovany-welch-chouette-frontend-tls
  rules:
    - host: staging.chouette.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: staging-geovany-welch-chouette-frontend-service
                port:
                  number: 3000
